import styles from "./Toast.module.css";

const Toast = ({ positionX, positionY, toastType, duration }) => {
    return (
        <div className={styles["toast-container"]}>

        </div>
    )
}

export default Toast;